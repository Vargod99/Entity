I built an entity extraction pipeline to extract key entities using different llms. 
I then compared the efficency of the llms and collectively came up with a common set of entities extract from all the llms. 
Finally, I ran a sentiment anaylsis to compare the news associated with entities.
